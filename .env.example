# =============================================================================
# Seedarr - Configuration
# =============================================================================
# Copiez ce fichier vers .env : cp .env.example .env
# Toute la configuration se fait ensuite via l'interface web /settings
# =============================================================================

# Base de donnees (ne pas modifier)
DATABASE_URL=sqlite:////app/backend/data/seedarr.db

# Port de l'application (defaut: 8000)
APP_PORT=8000

# Chemin vers vos fichiers media (sera monte dans /media dans Docker)
# Exemples: ./media, /mnt/nas/media, D:/Videos
MEDIA_PATH=./media

# Timezone
TZ=Europe/Paris
